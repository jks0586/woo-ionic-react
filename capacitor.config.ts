import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Woo_ionic_react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
